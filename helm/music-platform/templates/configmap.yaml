apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "music-platform.configMapName" . }}
  labels:
    {{- include "music-platform.labels" . | nindent 4 }}
data:
  POSTGRES_DB: {{ .Values.db.name | quote }}
  POSTGRES_USER: {{ .Values.db.user | quote }}
